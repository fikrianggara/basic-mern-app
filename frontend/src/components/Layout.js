import { Outlet } from "react-router-dom";

import React from "react";

//general parent component
// that act as the general UI layout of the app
const Layout = () => {
  return <Outlet />;
};

export default Layout;
